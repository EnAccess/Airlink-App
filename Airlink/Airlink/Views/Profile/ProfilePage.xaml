<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Airlink.Views.Profile.ProfilePage"
             NavigationPage.HasNavigationBar="False"
             Shell.BackgroundColor="{StaticResource Primary}">
    <StackLayout BackgroundColor="{StaticResource Color_2}">
        <Grid  RowDefinitions="Auto,Auto,*">
            <StackLayout Grid.Row="0" Margin="0,-100">
                <Grid RowDefinitions="Auto,*">

                    <Path 
                        Grid.RowSpan="1"
                        Grid.ColumnSpan="1"
                        HorizontalOptions="FillAndExpand"
                        VerticalOptions="FillAndExpand"
                        Fill="{AppThemeBinding Dark=#1B75BA, Light=#1B75BA}"
                        Data="M0,0H720V217.216s-184.713,64.169-347.376,64.169S0,217.216,0,217.216V0"
                        Aspect="Uniform" Scale="1.01" />
                    <StackLayout  Grid.Row="1" Margin="0,-120,0,0">
                        <Frame BackgroundColor="{StaticResource Background}" CornerRadius="10" Margin="20,0" RotationY="20">
                            <Grid ColumnDefinitions="Auto,*,Auto">
                                <Image Source="profile.png" Grid.Column="0" HeightRequest="40"/>
                                <StackLayout Grid.Column="1">
                                    <Grid RowDefinitions="*,Auto">
                                        <Label Text="Zilic Zilic" TextColor="{StaticResource Black}" HorizontalOptions="CenterAndExpand" VerticalOptions="Center"
                                               Scale="2"/>
                                        <Label Text="+255 714 340 649" TextColor="{StaticResource Color_1}" HorizontalOptions="CenterAndExpand"
                                               VerticalOptions="CenterAndExpand" Grid.Row="1"/>
                                    </Grid>
                                </StackLayout>
                                <ImageButton Source="profile.png" HeightRequest="40" Grid.Column="2" Clicked="ImageButton_Clicked"/>
                            </Grid>
                        </Frame>
                    </StackLayout>
                </Grid>
            </StackLayout>
        </Grid>
    </StackLayout>
</ContentPage>